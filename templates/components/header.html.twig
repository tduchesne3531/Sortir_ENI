<header class="w-[95vw] mb-8 border-2 border-b-amber-600 rounded-b-full py-4 px-6">
    <div class="flex items-center justify-between px-6 py-4 w-[90vw]">
        <div class="flex items-center gap-4">
            <a href="{{ path('participant_list') }}">
                <img src="{{ asset('images/logo_sortir_proposal.png') }}" alt="Sortir.com logo" class="w-20">
            </a>
            <h1 class="text-xl sm:text-2xl font-bold">Sortir.com</h1>
        </div>

        {{ include('components/menu.html.twig') }}
    </div>
</header>

