{% extends 'base.html.twig' %}

{% block title %}{{ site.name ? 'Modifier' : 'Créer' }} un site{% endblock %}

{% block body %}
    <h1>{{ site.name ? 'Modifier' : 'Créer' }} un site</h1>

    {{ form_start(form) }}
    {{ form_row(form.name) }}
    {{ form_end(form) }}
    <button class="btn btn-primary">{{ site.name ? 'Modifier' : 'Créer' }}</button>
{% endblock %}