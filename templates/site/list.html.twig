{% extends 'base.html.twig' %}

{% block title %}Liste des sites !
{% endblock %}

{% block body %}

	<div class="example-wrapper">
		<div class="search-bar">
			<form method="get" action="{{ path('site_search') }}">
				<label for="search">Le nom contient :</label>
				<input type="text" id="search" name="search" placeholder="Rechercher un site" value="{{ app.request.query.get('search') }}">
				<button type="submit" class="btn btn-primary">Rechercher</button>
			</form>
		</div>

		{% include 'components/sites.html.twig' %}

		<div class="add-button">
			<a href="{{ path('site_add') }}" class="btn btn-success">Ajouter</a>
		</div>
	</div>
{% endblock %}
